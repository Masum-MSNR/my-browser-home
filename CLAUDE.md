# My Chrome Tab

Chrome extension (Manifest V3) — custom new tab page with clock, search, shortcuts, bookmarks, themes, and Firebase sync. Vanilla JS/CSS/HTML, no build tools.

## Always

- After every code change, commit with a descriptive message and push immediately:
  ```
  git add <files> && git commit -m "..." && git push origin main
  ```
- Use `@'...'@` heredoc syntax for commit messages
- Co-Authored-By line: `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>`
